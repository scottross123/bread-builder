import OverallTable from "./OverallTable";

const BreadFormula = () => {
    return (
        <div className="flex">
            <OverallTable />
        </div>
    );
};

export default BreadFormula;
